import { BeatLoader } from 'react-spinners';
import { StyledLoader } from './Loader.style';

const Loader = () => {
    return (
        <StyledLoader>
            <BeatLoader />
        </StyledLoader>
    );
};

export default Loader;
